<%= form_with(model: user, id: 'user_login_form', url: '/login') do |f| %>

  <% if user.errors.any? %>
    <%= render partial: 'errors', locals: { user: user } %>
  <% end %>

  <p><%= f.text_field :email %></p>
  <p><%= f.password_field :password %></p>

  <%= f.submit 'Login', class: 'submit' %>
<% end %>
