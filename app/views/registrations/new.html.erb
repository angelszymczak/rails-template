<h1>Registrations#new</h1>

<%= form_with(model: @user, id: 'user_registration_form', url: '/signup') do |f| %>
  <p><%= f.text_field :email %></p>
  <p><%= f.password_field :password %></p>

  <%= f.submit 'Register', class: 'submit' %>
<% end %>
